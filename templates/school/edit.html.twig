{% extends 'base.html.twig' %}

{% block title %}Modifier {{ school.title }}{% endblock %}

{% block body %}
    <h1>Modifier {{ school.title }}</h1>

    {{ include('school/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('teacher_schools') }}">back to home</a>

    {#{{ include('school/_delete_form.html.twig') }}#}
{% endblock %}
