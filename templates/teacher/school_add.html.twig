{% extends 'teacher/base.html.twig' %}

{% block title %} Ajouter un employeur {% endblock %}

{% block body %}
<div class="row centered">
    <div class="col">
        <div class="container mt4">
            <h1>Ajouter un employeur</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success"><i class="fas fa-external-link-square-alt"></i> Confirmez le lien</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
    
{% endblock %}