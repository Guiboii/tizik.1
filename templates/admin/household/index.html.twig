{% extends 'admin/base.html.twig' %}

{% block title %}Administration des foyers{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="my-5">Gestion des foyers</h1>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom du foyer</th>
                <th>Utilisateurs
                    <th>Prénom</th>
                    <th>Email</th>
                </th>
                <th>Ville</th>
                <th>CP</th>
            </tr>
        </thead>
        <tbody>
            {% for household in households %}
            <tr>
                <td>{{ household.id }}</td>
                <td>{{ household.fullName }}</td>
                {#<td>{% for user in household.student %}
                    <td><img src="{{user.picture}}" alt="" class="avatar avatar-mini mr-2">{{user.firstName}}</td>
                    <td>{{ user.email }}</td>
                {% endfor %}</td>#}
                <td>Des utilisateurs</td>
                {#<td>{{household.city}}</td>
                <td>{{household.zipcode}}</td>#}
                <td><a href="#" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                <a href="#" class="btn btn-danger"><i class="fas fa-trash"></i></a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
